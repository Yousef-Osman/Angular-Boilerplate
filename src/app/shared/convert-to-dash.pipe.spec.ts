import { ConvertToDashPipe } from './convert-to-dash.pipe';

describe('ConvertToSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToDashPipe();
    expect(pipe).toBeTruthy();
  });
});
